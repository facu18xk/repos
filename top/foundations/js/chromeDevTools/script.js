let mainTitle = document.querySelector("h1");
let newTitle = "What's up"
if (mainTitle.innerText != "") mainTitle.innerText = newTitle;
else mainTitle.innerText = "Hasanabi head";
for (let i = 0; i < 10; i++) {
    console.log(i);
}